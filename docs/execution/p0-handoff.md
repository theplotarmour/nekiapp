# P0 handoff and explicit P1 entry

**2026-09-15:** The user instructed “complete p0 and start p1” after receiving the outstanding-gate report. This authorizes beginning the independent P1 platform foundation now. It changes the blanket sequencing restriction for that bounded work; it does **not** fabricate the missing P0 approvals or certify P0 complete. D1–D7 and live-funds/privacy/eligibility boundaries remain unchanged.

## Engineering handoff available

Use the [P0 register](phase-0.md) for detailed evidence: 324 draft operation contracts, state/permission drafts, eight source inspections, current Flutter compatibility and browser probe, PostgreSQL cursor/recovery proof, semantic tokens and contrast measurements, nine-journey prototype, operating-policy draft and sized P1 backlog. ADR-002/014 and their associated source sections have been reconciled for the tested technical decisions.

The first application slice uses accepted ADR-004's Python 3.12/FastAPI/SQLAlchemy/Alembic architecture. Framework/lifecycle/configuration/migration work can proceed without inventing volunteer ages, pickup boundaries, retention durations or financial approval thresholds. Synthetic integration tests are isolated from existing databases. No absent endpoint is represented as implemented.

## Unclosed P0 obligations carried forward

| Requirement | Blocks | Evidence needed |
|---|---|---|
| Named provider/legal/operating owners and dates | Account onboarding, staffed operations and formal P0 closure | Actual names, ownership acceptance and dates |
| Open product/financial/privacy policy values | Dependent domain schema/commands and public policy promises | Recorded selection/revision and appropriate review; see foundation draft and transition decisions |
| Complete reconciled domain DDL and per-event payload registry | Domain implementation | Reviewed relationships, constraints, typed payloads and executable negative/concurrency cases |
| SMS/payment/maps/media/Redis/mobile/managed-deployment validation | Corresponding integration and staging acceptance | Real provider/platform evidence with approved accounts and scoped configuration |
| Complete design/assets/accessibility and participant review | Design acceptance and P0 closure | Actual licensed assets, remaining layout/assistive-tech checks and real participant observations |

These obligations remain open in the P0 register. Foundation code, local tests, a green CI run or a changed document label cannot sign them off. The seven Razorpay sandbox cases and live-funds legal gate remain separate from this application bootstrap.

## Contract and schema ownership

`api/openapi.json` becomes the generated source for implemented platform routes; the P0 `docs/api/openapi.json` remains a clearly labeled draft for unimplemented domain operations. Domain implementation must reconcile and transfer each contract rather than maintaining two independent editable schemas. The initial migration covers event-storage structure only; required handler contents, typed domain payloads and application grant rules are not inferred from JSON columns.

**Status:** P0 handoff recorded; full P0 exit remains open. P1 started, not completed. See [P1 execution](phase-1.md) for actual implemented scope and test evidence.
